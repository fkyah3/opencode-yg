## WeakRef（弱引用）<- RefCounted（引用计数）

弱引用可以在不增加引用计数器的情况下持有 RefCounted 对象。可以使用 `@GlobalScope.weakref` 从 Object 创建弱引用。如果此对象不是引用，弱引用仍然有效，但对对象没有影响。弱引用在多个类具有相互引用的变量时很有用。没有弱引用，使用这些类可能导致内存泄漏，因为两个引用相互阻止对方释放。将部分变量设为弱引用可以防止这种循环依赖，并允许引用被释放。

**方法（Methods）：**
- get_ref() -> Variant —— 获取引用
