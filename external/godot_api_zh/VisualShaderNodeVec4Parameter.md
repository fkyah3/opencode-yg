## VisualShaderNodeVec4Parameter（可视化着色器节点Vec4参数） <- VisualShaderNodeParameter（可视化着色器节点参数）

着色器语言中对应 `uniform vec4`。

**属性（Props）：**
- default_value: Vector4 = Vector4(0, 0, 0, 0)
- default_value_enabled: bool = false
