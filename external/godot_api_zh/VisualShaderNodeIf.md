## VisualShaderNodeIf（可视化着色器节点条件判断） <- VisualShaderNode（可视化着色器节点）

此可视化着色器节点有六个输入端口：
- 端口 **1** 和 **2** 提供待比较的两个浮点数 `a` 和 `b`。
- 端口 **3** 为容差值，允许将相近的浮点数视为相等。
- 端口 **4**、**5** 和 **6** 分别为可能输出的结果，对应 `a == b`、`a > b` 或 `a < b` 时返回。
