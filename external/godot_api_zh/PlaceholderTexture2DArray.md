## PlaceholderTexture2DArray（占位纹理2D数组）<- PlaceholderTextureLayered（占位分层纹理）

此类在以下两种情况下加载使用 Texture2D 子类的项目时使用：- 以专用服务器模式运行导出的项目时，仅保留纹理的尺寸（因为游戏玩法或其他元素定位可能依赖它们）。这可以显著减小导出的 PCK 文件大小。- 当由于使用不同的引擎版本或构建（例如模块禁用）而导致此子类缺失时。**注意：** 此类型不适合用作实际渲染纹理。不能保证在着色器或材质中像真正的纹理一样工作（例如计算 UV 时）。
