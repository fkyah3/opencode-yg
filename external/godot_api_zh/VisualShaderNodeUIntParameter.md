## VisualShaderNodeUIntParameter（无符号整数参数节点）<- VisualShaderNodeParameter（参数节点）

无符号 int 类型的 VisualShaderNodeParameter。提供对接受值范围进行额外自定义设置。

**属性（Props）：**
- default_value: int = 0 —— 默认值
- default_value_enabled: bool = false —— 是否启用默认值
