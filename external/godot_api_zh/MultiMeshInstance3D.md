## MultiMeshInstance3D（3D多网格实例） <- GeometryInstance3D（3D几何体实例）

MultiMeshInstance3D 是一个专用节点，用于基于 MultiMesh 资源实例化 GeometryInstance3D。这对于优化大量相同网格实例（例如森林中的树木或草丛）的渲染非常有用。

**属性（Props）：**
- multimesh: MultiMesh —— 多网格
