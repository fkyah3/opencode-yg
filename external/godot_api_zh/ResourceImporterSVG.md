## ResourceImporterSVG（SVG资源导入器）<- ResourceImporter（资源导入器）

此导入器导入 DPITexture 资源。另请参见 ResourceImporterTexture 和 ResourceImporterImage。

**属性（Props）：**
- base_scale: float = 1.0 —— 基础缩放
- color_map: Dictionary = {} —— 颜色映射
- compress: bool = true —— 是否压缩
- saturation: float = 1.0 —— 饱和度
