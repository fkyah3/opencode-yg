## VisualShaderNodeSample3D（可视化着色器节点采样3D） <- VisualShaderNode（可视化着色器节点）

一个虚拟类，请改用其派生类。

**属性（Props）：**
- source: int (VisualShaderNodeSample3D.Source) = 0

**枚举（Enums）：**
**Source（来源）：** SOURCE_TEXTURE=0, SOURCE_PORT=1, SOURCE_MAX=2
