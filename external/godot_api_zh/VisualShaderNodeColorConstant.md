## VisualShaderNodeColorConstant（颜色常量节点）<- VisualShaderNodeConstant（常量节点）

具有两个输出端口，分别表示 Color 的 RGB 和 alpha 通道。着色器语言中对应 `vec3 rgb` 和 `float alpha`。

**属性（Props）：**
- constant: Color = Color(1, 1, 1, 1) —— 颜色常量值
