## RichTextEffect（富文本效果） <- Resource（资源）

RichTextLabel 的自定义效果，可在 RichTextLabel 检视器中加载或使用 `RichTextLabel.install_effect` 加载。**注意：** 要使 RichTextEffect 可用，必须在脚本中定义一个名为 `bbcode` 的成员变量作为 BBCode 标签。**注意：** 只要 RichTextLabel 包含至少一个 RichTextEffect，除非项目暂停，否则它将持续处理该效果。这可能会对电池寿命产生负面影响。
