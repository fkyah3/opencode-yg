## ResourceImporter（资源导入器）<- RefCounted（引用计数）

这是 Godot 资源导入器的基类。要使用编辑器插件实现自定义资源导入器，请参见 EditorImportPlugin。

**枚举（Enums）：**
**ImportOrder（导入顺序）：** IMPORT_ORDER_DEFAULT=0 —— 默认导入顺序, IMPORT_ORDER_SCENE=100 —— 场景导入顺序
