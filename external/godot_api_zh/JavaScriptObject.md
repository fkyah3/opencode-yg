## JavaScriptObject（JavaScript对象）<- RefCounted（引用计数）

JavaScriptObject 用于与通过 `JavaScriptBridge.get_interface`、`JavaScriptBridge.create_object` 或 `JavaScriptBridge.create_callback` 获取或创建的 JavaScript 对象进行交互。**注意：** 仅在 Web 平台上可用。
