## ReferenceRect（参考矩形） <- Control（控件）

一个矩形框，仅在其四周显示彩色边框（参见 `Control.get_rect`）。可用于可视化 Control 节点的范围，供测试使用。

**属性（Props）：**
- border_color: Color = Color(1, 0, 0, 1) —— 边框颜色
- border_width: float = 1.0 —— 边框宽度
- editor_only: bool = true —— 是否仅在编辑器中显示
