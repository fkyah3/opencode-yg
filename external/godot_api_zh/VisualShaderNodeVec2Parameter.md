## VisualShaderNodeVec2Parameter（可视化着色器节点Vec2参数） <- VisualShaderNodeParameter（可视化着色器节点参数）

着色器语言中对应 `uniform vec2`。

**属性（Props）：**
- default_value: Vector2 = Vector2(0, 0)
- default_value_enabled: bool = false
