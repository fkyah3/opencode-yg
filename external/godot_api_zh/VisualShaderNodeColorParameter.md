## VisualShaderNodeColorParameter（可视化着色器节点颜色参数） <- VisualShaderNodeParameter（可视化着色器节点参数）

着色器语言中对应 `uniform vec4`。

**属性（Props）：**
- default_value: Color = Color(1, 1, 1, 1)
- default_value_enabled: bool = false
