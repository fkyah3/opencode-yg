## PopupPanel（弹出面板） <- Popup（弹出窗口）

带有可配置面板背景的弹出窗口。添加到该节点的任何子控件都会被拉伸以适应面板尺寸（类似于 PanelContainer 的工作方式）。如果需要创建窗口，请参阅 Window。

**属性（Props）：**
- transparent: bool = true —— 是否透明
- transparent_bg: bool = true —— 背景是否透明
