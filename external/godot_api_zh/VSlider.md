## VSlider（垂直滑块） <- Slider（滑块）

垂直滑块，通过沿垂直轴拖动拖动条来调整值。它是基于 Range 的控件，范围从底部（最小值）到顶部（最大值）。注意此方向与 VScrollBar 相反。

**属性（Props）：**
- size_flags_horizontal: int (Control.SizeFlags) = 0 —— 水平尺寸标志
- size_flags_vertical: int (Control.SizeFlags) = 1 —— 垂直尺寸标志
