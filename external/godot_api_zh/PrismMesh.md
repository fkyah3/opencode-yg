## PrismMesh（棱柱网格）<- PrimitiveMesh（基本网格）

表示棱柱形状的 PrimitiveMesh 的类。

**属性（Props）：**
- left_to_right: float = 0.5 —— 从左到右的比例
- size: Vector3 = Vector3(1, 1, 1) —— 尺寸
- subdivide_depth: int = 0 —— 深度细分
- subdivide_height: int = 0 —— 高度细分
- subdivide_width: int = 0 —— 宽度细分
