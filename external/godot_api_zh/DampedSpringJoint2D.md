## DampedSpringJoint2D（2D阻尼弹簧关节） <- Joint2D（2D关节）

一种物理关节，以弹簧力连接两个2D物理体。其行为类似于始终希望拉伸到指定长度的弹簧。

**属性（Props）：**
- damping: float = 1.0 —— 阻尼
- length: float = 50.0 —— 长度
- rest_length: float = 0.0 —— 静止长度
- stiffness: float = 20.0 —— 刚度
