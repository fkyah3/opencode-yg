## VisualShaderNodeVarying（Varying变量节点）<- VisualShaderNode（可视化着色器节点）

Varying 值是在着色器函数之间传递的着色器变量，例如从顶点着色器传递到片段着色器。

**属性（Props）：**
- varying_name: String = "[None]" —— Varying变量名
- varying_type: int (VisualShader.VaryingType) = 0 —— Varying变量类型
