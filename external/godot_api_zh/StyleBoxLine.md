## StyleBoxLine（线条样式盒） <- StyleBox（样式盒）

一种显示单条指定颜色和粗细线条的 StyleBox。线条可以是水平或垂直的。适用于分隔符。

**属性（Props）：**
- color: Color = Color(0, 0, 0, 1) —— 颜色
- grow_begin: float = 1.0 —— 起始增长
- grow_end: float = 1.0 —— 结束增长
- thickness: int = 1 —— 厚度
- vertical: bool = false —— 是否垂直
