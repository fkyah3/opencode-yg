## VScrollBar（垂直滚动条） <- ScrollBar（滚动条）

垂直滚动条，通常用于导航超出控件可见高度的内容。它是一个基于 Range 的控件，方向为从上（最小值）到下（最大值）。注意此方向与 VSlider 相反。

**属性（Props）：**
- size_flags_horizontal: int (Control.SizeFlags) = 0 —— 水平尺寸标志
- size_flags_vertical: int (Control.SizeFlags) = 1 —— 垂直尺寸标志
