## VisualShaderNodeBooleanParameter（可视化着色器节点布尔参数） <- VisualShaderNodeParameter（可视化着色器节点参数）

着色器语言中对应 `uniform bool`。

**属性（Props）：**
- default_value: bool = false
- default_value_enabled: bool = false
