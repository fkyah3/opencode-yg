## ColorPalette（颜色调色板） <- Resource（资源）

ColorPalette 资源用于存储和管理颜色集合。该资源适用于需要预定义颜色集的场景，如创建主题、设计用户界面或管理游戏资源。内置的 ColorPicker 控件无需额外代码即可使用 ColorPalette。

**属性（Props）：**
- colors: PackedColorArray = PackedColorArray() —— 颜色数组
