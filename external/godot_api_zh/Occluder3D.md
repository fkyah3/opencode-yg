## Occluder3D（3D遮挡器） <- Resource（资源）

Occluder3D 存储一个遮挡器形状，可用于引擎的遮挡剔除系统。关于遮挡剔除的设置说明，请参见 OccluderInstance3D 的文档。

**方法（Methods）：**
- get_indices() -> PackedInt32Array —— 获取索引数组
- get_vertices() -> PackedVector3Array —— 获取顶点数组
