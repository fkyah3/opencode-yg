## VisualShaderNodeVaryingGetter（可视化着色器节点Varying获取器） <- VisualShaderNodeVarying（可视化着色器节点Varying）

输出着色器中定义的 varying 的值。你需要首先创建一个可在给定函数中使用的 varying，例如片段着色器中的 varying 获取器要求 varying 的模式设置为 `VisualShader.VARYING_MODE_VERTEX_TO_FRAG_LIGHT`。
