## VisualShaderNodeVaryingSetter（可视化着色器节点Varying设置器） <- VisualShaderNodeVarying（可视化着色器节点Varying）

向着色器中定义的 varying 输入一个值。你需要首先创建一个可在给定函数中使用的 varying，例如片段着色器中的 varying 设置器要求 varying 的模式设置为 `VisualShader.VARYING_MODE_FRAG_TO_LIGHT`。
