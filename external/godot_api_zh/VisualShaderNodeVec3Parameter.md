## VisualShaderNodeVec3Parameter（可视化着色器节点Vec3参数） <- VisualShaderNodeParameter（可视化着色器节点参数）

着色器语言中对应 `uniform vec3`。

**属性（Props）：**
- default_value: Vector3 = Vector3(0, 0, 0)
- default_value_enabled: bool = false
