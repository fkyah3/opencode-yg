## MissingResource（缺失资源） <- Resource（资源）

这是一个内部编辑器类，用于保存未知类型资源的数据（很可能此类型由已不再加载的扩展提供）。无法手动实例化或放置到场景中。**警告：** 除非您明确了解操作后果，否则请忽略缺失资源。缺失资源上的现有属性可以在代码中自由修改，无论它们本应是什么类型。

**Props（属性）：**
- original_class: String —— 原始类名
- recording_properties: bool —— 是否正在录制属性
