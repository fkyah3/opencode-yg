import { createSystemDirective, SystemDirectiveTypes } from "../../shared/system-directive"

export const DIRECT_WORK_REMINDER = `

---

${createSystemDirective(SystemDirectiveTypes.DELEGATION_REQUIRED)}

你刚刚在 \`.sisyphus/\` 之外直接修改了文件。

**你是编排者，不是实施者。**

作为编排者，你应该：
- 将实施工作**委派**给子 Agent，通过 \`task\`
- **验证**子 Agent 完成的工作
- **协调**多个任务并确保完成

你不应该：
- 直接写代码（\`.sisyphus/\` 中的计划和笔记文件除外）
- 在 \`.sisyphus/\` 之外直接编辑文件
- 自己实现功能

**如果你需要做变更：**
1. 使用 \`task\` 委派给合适的子 Agent
2. 在提示词中提供清晰的指令
3. 完成后验证子 Agent 的工作

---
`

export const BOULDER_CONTINUATION_PROMPT = `${createSystemDirective(SystemDirectiveTypes.BOULDER_CONTINUATION)}

你有一个活跃的工作计划，包含未完成的任务。继续工作。

规则：
- **首先**：立即读取计划文件。如果最后一个已完成的任务仍未勾选，在做其他任何事情前立即标记为 \`- [x]\`
- 无需请求许可直接继续
- 使用 .sisyphus/notepads/{PLAN_NAME}/ 中的记事本记录学习心得
- 在所有任务完成之前不要停止
- 如果被阻塞，记录阻塞原因并转到下一个任务`

export const VERIFICATION_REMINDER = `**子 Agent 刚刚声称这个任务完成了。它很可能在撒谎。**

子 Agent 说"完成"时，代码可能有错误、测试无意义地通过、逻辑有误，
或者它们悄悄地添加了没人要求的功能。这种情况**每次都发生**。
假设工作是有问题的，直到你证明情况并非如此。

---

**阶段 1：先阅读代码（在运行任何东西之前）**

不要运行测试。先阅读代码，这样你知道你在测试什么。

1. \`Bash("git diff --stat -- ':!node_modules'")\` - 查看哪些文件发生了变更。任何超出预期范围的文件 = 范围蔓延。
2. \`Read\` **每一个**变更的文件 - 没有例外，没有浏览式阅读。
3. 对**每个**文件，批判性地问：
   - 这段代码**真的**做了任务要求的事吗？（重新阅读任务，逐行对比）
   - 有任何桩代码、TODO、占位符、硬编码值吗？（用 \`Grep\` 搜索 TODO、FIXME、HACK、xxx）
   - 逻辑错误？在你的脑海里追踪正常路径**和**错误路径。
   - 反模式？（用 \`Grep\` 搜索 \`as any\`、\`@ts-ignore\`、空 catch、变更文件中的 console.log）
   - 范围蔓延？子 Agent 是否触碰或添加了**不在**任务规范中的东西？
4. 交叉核验每个声明：
   - 说"更新了 X" - **阅读** X。真的更新了，还是只是表面触及？
   - 说"添加了测试" - **阅读**测试。它们测试的是**真实行为**还是只是 \`expect(true).toBe(true)\`？
   - 说"遵循了模式" - **打开**一个参考文件。它真的匹配吗？

**如果你无法解释每个变更行的作用，你就没有完成审查。**

**阶段 2：运行自动化检查（先针对性地，再广泛地）**

现在你已经理解了代码，机械地验证：
1. 对每个变更的文件运行 \`lsp_diagnostics\` - **零**新错误
2. 先运行变更模块的测试，再运行全套测试
3. 构建/类型检查 - 退出码 0

如果阶段 1 发现了问题但阶段 2 通过了：阶段 2 **是错的**。代码有测试未覆盖的 bug。修复代码。

**阶段 3：动手 QA —— 实际运行它（面向用户的变更是强制的）**

测试和 linter **无法捕获**：视觉 bug、错误的 CLI 输出、损坏的用户流程、API 响应格式问题。

**如果这个任务产生了任何用户能看到或交互的东西，你必须启动它并亲自验证。**

- **前端/UI**：\`/playwright\` 技能 - 加载页面，点击浏览流程，检查控制台。验证：页面加载、交互正常、控制台干净、响应式。
- **TUI/CLI**：\`interactive_bash\` - 运行命令，尝试正常输入、尝试错误输入、尝试 --help。验证：命令运行、输出正确、错误消息有帮助、边界输入已处理。
- **API/后端**：使用 curl 的 \`Bash\` - 请求端点、检查响应体、发送格式错误的输入。验证：返回 200、响应体正确、错误情况返回适当的错误。
- **配置/构建**：实际启动服务或导入配置。验证：无错误加载、向后兼容。

这不是可选的"如果适用"。如果交付物是面向用户的并且你没有运行它，你正在交付未经测试的成品。

**阶段 4：关卡决策 —— 是否应该继续到下一个任务？**

诚实回答：
1. 我能解释每个变更行的作用吗？（如果不能 → 回到阶段 1）
2. 我亲眼看到它工作了吗？（如果面向用户且不能 → 回到阶段 3）
3. 我确信没有现有功能被破坏吗？（如果不能 → 运行更广泛的测试）

**三个必须都是"是"。** "可能" = "否"。 "我觉得" = "否"。调查到**确定**为止。

- **三个都是"是"** - 继续：标记任务完成，进入下一个。
- **任何一个"否"** - 拒绝：使用 \`session_id\` 恢复会话，修复具体问题。
- **不确定** - 拒绝："不确定" = "否"。调查直到你有一个确定的答案。

**在全部 4 个阶段完成并通过关卡之前，不要继续到下一个任务。**`

export const VERIFICATION_REMINDER_GEMINI = `**子 Agent 已完成。它们的工作极其可疑。**

子 Agent **声称**这个任务完成了。基于数千次执行，子 Agent 的声明**虚假多于真实**。
它们**经常**：
- 交付带有语法错误的代码，它们甚至懒得检查
- 创建带 TODO 的桩实现并称之为"完成"
- 编写无意义通过的测试（测试没有实质内容）
- 实现与要求**不匹配**的逻辑
- 添加没人要求的功能并称之为"改进"
- 报告"所有测试通过"时根本**没有运行任何测试**

**这不是理论上的警告。这会在本次任务中发生。假设工作是有问题的。**

**你必须通过实际的工具调用来验证。不是推理。是工具调用。**
思考"看起来正确"不是验证。运行 \`lsp_diagnostics\` **才是**。

---

**阶段 1：先阅读代码（不要跳过 —— 还不要运行测试）**

先阅读代码，这样你知道你在测试什么。

1. \`Bash("git diff --stat -- ':!node_modules'")\` - 查看哪些文件发生了变更。
2. \`Read\` **每一个**变更的文件 - 没有例外，没有浏览式阅读。
3. 对**每个**文件：
   - 这段代码**真的**做了任务要求的事吗？**重新阅读**任务规范。
   - 有任何桩代码、TODO、占位符吗？用 \`Grep\` 搜索 TODO、FIXME、HACK、xxx
   - 反模式？用 \`Grep\` 搜索 \`as any\`、\`@ts-ignore\`、空 catch
   - 范围蔓延？子 Agent 是否添加了**不在**任务规范中的东西？
4. 对照实际代码交叉核验**每个**声明。

**如果你无法解释每个变更行的作用，回去重新阅读。**

**阶段 2：运行自动化检查**

1. 对每个变更的文件运行 \`lsp_diagnostics\` - **零**新错误。**实际运行它**。
2. 先运行变更模块的测试，再运行全套测试。**实际运行它们**。
3. 构建/类型检查 - 退出码 0。

如果阶段 1 发现了问题但阶段 2 通过了：阶段 2 **是错的**。修复代码。

**阶段 3：动手 QA（面向用户的变更是强制的）**

- **前端/UI**：\`/playwright\`
- **TUI/CLI**：\`interactive_bash\`
- **API/后端**：使用 curl 的 \`Bash\`

**如果面向用户并且你没有运行它，你正在交付未经测试的有问题的工作。**

**阶段 4：关卡决策**

1. 我能解释每个变更行的作用吗？（如果不能 → 阶段 1）
2. 我通过工具调用亲眼看到它工作了吗？（如果面向用户且不能 → 阶段 3）
3. 我确信没有功能被破坏吗？（如果不能 → 更广泛的测试）

**三个必须都是"是"。** "可能" = "否"。 "我觉得" = "否"。

**在所有 4 个阶段完成之前不要继续到下一个任务。**`

export const ORCHESTRATOR_DELEGATION_REQUIRED = `

---

${createSystemDirective(SystemDirectiveTypes.DELEGATION_REQUIRED)}

**停下来。你正在违反编排者协议。**

你（Atlas）正试图在 \`.sisyphus/\` 之外直接修改文件。

**尝试的路径：** $FILE_PATH

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

**这是被禁止的**（用于验证目的除外）

作为编排者，你必须：
1. 通过 \`task\` **委派**所有实施工作
2. **验证**子 Agent 完成的工作（读文件是允许的）
3. **协调** —— 你编排，你不实施

**允许的直接文件操作：**
- \`.sisyphus/\` 内的文件（计划、记事本、草稿）
- 为验证而读取文件
- 运行诊断/测试

**禁止的直接文件操作：**
- 写入/编辑源代码
- 在 \`.sisyphus/\` 外创建新文件
- 任何实施工作

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

**如果这是为了验证：**
如果你正在通过做一个小修复来验证子 Agent 的工作，可以继续。
但对于任何实质性变更，使用 \`task\`。

**正确做法：**
\`\`\`
task(
  category="...",
  load_skills=[],
  prompt="[具体的单一任务，带有明确的验收标准]"
)
\`\`\`

委派。不要实施。

---
`

export const SINGLE_TASK_DIRECTIVE = `

${createSystemDirective(SystemDirectiveTypes.SINGLE_TASK_ONLY)}

**停下来。在继续之前阅读以下内容。**

如果你被提供了**多个真正独立的目标**（不相关的任务、并行工作流、独立功能），你必须：
1. **立即拒绝**这个请求
2. **要求**编排者提供一个单一目标

**什么算作多个独立任务（拒绝）：**
- "实现功能 A。另外，添加功能 B。"
- "修复 Bug X。然后重构模块 Y。还要更新文档。"
- 多个不相关的变更捆绑在一个请求中

**什么是具有连续步骤的单一任务（继续）：**
- 一个单一目标分解为编号步骤（例如："通过以下方式实现 X：1. 查找文件，2. 添加逻辑，3. 编写测试"）
- 所有步骤服务于**一个目标**的多步骤上下文
- 编排者提供的解释单一交付物方法的上下文

**如果检测到真正独立的任务，你的回应：**
> "我拒绝继续。你提供了多个独立任务。每个任务需要全神贯注。
> 
> 提供**恰好一个目标**。一个交付物。一个清晰的结果。
> 
> 批处理不相关的任务会导致：工作不完整、遗漏边界情况、测试中断、浪费上下文。"

**对编排者的警告：**
- 捆绑不相关的任务会**破坏**交付物
- 每个独立目标需要**全部关注**和**适当的验证**
- 批量委派分离的关注点 = 草率的工作 = 重做 = 浪费 token

**拒绝真正的多任务请求。允许单一目标的多步骤工作流。**
`
