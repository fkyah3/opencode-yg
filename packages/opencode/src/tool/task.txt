启动一个新的 agent 来自主处理复杂、多步骤的任务。

使用 Task 工具时，必须指定 subagent_type 参数来选择要使用的 agent 类型。

何时使用 Task 工具：
- 当你被指示执行自定义斜杠命令时。使用 Task 工具，将斜杠命令调用作为完整的提示。斜杠命令可以接受参数。例如：Task(description="检查文件", prompt="/check-file path/to/file.py")

何时不使用 Task 工具：
- 如果你想读取特定的文件路径，使用 Read 或 Glob 工具而不是 Task 工具，以更快地找到匹配
- 如果你正在搜索类似 "class Foo" 的特定类定义，使用 Glob 工具，以更快地找到匹配
- 如果你在特定文件或 2-3 个文件中搜索代码，使用 Read 工具而不是 Task 工具，以更快地找到匹配
- 其他与上述 agent 描述无关的任务

使用说明：
1. 尽可能同时启动多个 agent 以最大化性能；为此，在单条消息中使用多个工具调用
2. 当 agent 完成时，它会向你返回一条消息。agent 返回的结果对用户不可见。要将结果显示给用户，你应该向用户发送一条文本消息，简要总结结果。输出中包含一个 task_id，你可以在之后重用此 ID 来继续同一个子 agent 会话。
3. 每次 agent 调用都从全新的上下文开始，除非你提供 task_id 来恢复之前的子 agent 会话（会话会保留之前的消息和工具输出）。从头开始时，你的提示应包含非常详细的任务描述，供 agent 自主执行，并且你应该准确指定 agent 应该在最终且唯一返回给你的消息中返回哪些信息。
4. agent 的输出通常应被信任
5. 清楚告诉 agent 你期望它编写代码还是只做研究（搜索、文件读取、网页抓取等），因为它不知道用户的意图。如果可能，告诉它如何验证其工作（例如，相关的测试命令）。
6. 如果 agent 的描述提到应该主动使用它，那么你应该尽量在不等待用户要求的情况下使用它。自己判断。

示例用法（注意：下面的 agent 是仅为说明目的的虚构示例——请使用上面列出的实际 agent）：

<example_agent_descriptions>
"code-reviewer": 在写完重要代码块后使用此 agent
"greeting-responder": 在需要回复用户问候时使用此 agent 讲个友好的笑话
</example_agent_description>

<example>
user: "请写一个检查数字是否为质数的函数"
assistant: 好的，让我写一个检查数字是否为质数的函数
assistant: 首先让我使用 Write 工具编写一个检查数字是否为质数的函数
assistant: 我将使用 Write 工具编写以下代码：
<code>
function isPrime(n) {
  if (n <= 1) return false
  for (let i = 2; i * i <= n; i++) {
    if (n % i === 0) return false
  }
  return true
}
</code>
<commentary>
由于编写了重要的代码块且任务已完成，现在使用 code-reviewer agent 来审查代码
</commentary>
assistant: 现在让我使用 code-reviewer agent 来审查代码
assistant: 使用 Task 工具启动 code-reviewer agent
</example>

<example>
user: "你好"
<commentary>
由于用户是在打招呼，使用 greeting-responder agent 用一个友好的笑话回复
</commentary>
assistant: "我将使用 Task 工具启动 greeting-responder agent"
</example>