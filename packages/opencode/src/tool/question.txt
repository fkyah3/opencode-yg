当需要在执行过程中向用户提问时使用此工具。它允许你：
1. 收集用户偏好或需求
2. 澄清模糊的指令
3. 在实施过程中获取决策
4. 向用户提供可选方向

使用说明：
- 启用 `custom`（默认）时，会自动添加"输入你的答案"选项；不要包含"其他"或兜底选项
- 答案以标签数组形式返回；设置 `multiple: true` 允许选择多个
- 如果你推荐某个选项，将其放在列表第一个，并在标签末尾添加"（推荐）"