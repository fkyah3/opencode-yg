- 从指定 URL 获取内容
- 输入 URL 和可选格式参数
- 获取 URL 内容，转换为请求的格式（默认 markdown）
- 返回指定格式的内容
- 当你需要检索和分析网络内容时使用此工具

使用说明：
  - **重要**：如果其他工具提供更好的网页获取能力、更针对任务或有更少限制，优先使用那些工具
  - URL 必须是完整的有效 URL
  - HTTP URL 会自动升级为 HTTPS
  - 格式选项："markdown"（默认）、"text"或"html"
  - 此工具为只读，不会修改任何文件
  - 如果内容非常大，可能会被汇总返回