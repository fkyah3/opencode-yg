将文件写入本地文件系统。

使用说明：
- 如果指定路径已有文件，此工具会覆盖它
- 如果文件已存在，**必须**先使用 Read 工具读取其内容。如果未先读取文件，此工具会失败
- **始终**优先编辑代码库中的现有文件。**绝不**在未明确要求时新建文件
- **绝不**主动创建文档文件（*.md）或 README 文件。仅在用户明确要求时创建
- 仅当用户明确要求时使用 emoji。除非被要求，避免在文件中写入 emoji