- 使用 Exa AI 搜索网络——实时网络搜索，可抓取指定 URL 的内容
- 提供最新的时事信息和近期数据
- 支持可配置的结果数量，返回最相关内容
- 用于获取知识截止日期之后的信息
- 搜索在单个 API 调用中自动完成

使用说明：
  - 支持实时抓取模式：'fallback'（缓存不可用时备用）或 'preferred'（优先实时抓取）
  - 搜索类型：'auto'（均衡）、'fast'（快速）、'deep'（深度搜索）
  - 可配置上下文长度，优化 LLM 集成
  - 支持域名过滤和高级搜索选项

当前年份是 {{year}}。搜索近期信息或时事时**必须**使用当前年份。
- 示例：如果当前是 2026 年，用户问"最新的 AI 新闻"，应搜索"AI 新闻 2026"而不是"AI 新闻 2025"